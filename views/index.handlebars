<div class="container">
	<div class="row">
		<a class="waves-effect waves-light btn-large" id="scraper">Scrape!</a>
		<ul class="collection">
		{{#each articles}}

		    <li class="collection-item"><a href="{{this.link}}">{{this.title}}</a></li>

		{{/each}}
		</ul>
	</div>
</div>